
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>{{ App::make('TranslationHelper')->translateRecursive('members.name') }}</th>
                <th>{{ App::make('TranslationHelper')->translateRecursive('common.actions') }}</th>
            </tr>
        </thead>
        <tbody>
        @foreach( $##VARIABLE_PLURAL## as $##VARIABLE_SINGULAR## )
            <tr>
                <td>{{ $##VARIABLE_SINGULAR##->id }}</td>
                <td>{{ $##VARIABLE_SINGULAR##->name }}</td>
                <td class="table-small">
                    <div class="btn-group">
                        <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
                            {{ App::make('TranslationHelper')->translateRecursive('common.actions') }} <span class="caret"></span>
                        </button>
                        <ul class="dropdown-menu" role="menu">
                            <li>{{ HTML::linkRoute('admin.##VARIABLE_PLURAL##.edit', App::make('TranslationHelper')->translateRecursive('common.edit'), array($##VARIABLE_SINGULAR##->id), array('class' => 'btn btn-actions')) }}</li>
                            <li>{{ HTML::linkRoute('admin.##VARIABLE_PLURAL##.show', App::make('TranslationHelper')->translateRecursive('common.delete'), array($##VARIABLE_SINGULAR##->id), array('class' => 'btn btn-actions rest', 'data-method' => 'DELETE')) }}</li>
                        </ul>
                    </div>
                </td>
            </tr>
        @endforeach
        </tbody>
    </table>